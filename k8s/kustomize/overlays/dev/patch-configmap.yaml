apiVersion: v1
kind: ConfigMap
metadata:
  name: employee-management-config
data:
  ENVIRONMENT: dev
  LOG_LEVEL: debug
  ACCESS_TOKEN_EXPIRE_MINUTES: "480"
  CORS_ORIGINS: "http://employee.dev.sidhuge.xyz,http://localhost:8080"
